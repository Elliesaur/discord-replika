{
  "name": "discord-replika",
  "version": "1.0.0",
  "description": "Bot that allows you to use Replika.ai on Discord",
  "main": "./dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "discord.js": "^12.3.1",
    "dotenv": "^8.2.0",
    "puppeteer": "^5.3.0",
    "puppeteer-cluster": "^0.22.0",
    "typescript": "^4.0.2"
  },
  "devDependencies": {
    "@types/puppeteer": "^3.0.2"
  }
}
